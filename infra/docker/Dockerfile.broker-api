FROM node:22-alpine

WORKDIR /workspace

RUN corepack enable

COPY . .

RUN pnpm install --frozen-lockfile

EXPOSE 3000

CMD ["pnpm", "exec", "tsx", "--tsconfig", "apps/broker-api/tsconfig.json", "apps/broker-api/src/main.ts"]
