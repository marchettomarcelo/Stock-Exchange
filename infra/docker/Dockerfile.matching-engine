FROM node:22-alpine

WORKDIR /workspace

RUN corepack enable

COPY . .

RUN pnpm install --frozen-lockfile

EXPOSE 3001

CMD ["pnpm", "exec", "tsx", "--tsconfig", "apps/matching-engine/tsconfig.json", "apps/matching-engine/src/main.ts"]
