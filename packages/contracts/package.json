{
  "name": "@decade/contracts",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@decade/exchange-core": "workspace:*",
    "zod": "^3.24.1"
  }
}
