{
  "name": "@decade/application",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "lint": "eslint src",
    "test": "vitest run"
  },
  "dependencies": {
    "@decade/contracts": "workspace:*",
    "@decade/exchange-core": "workspace:*"
  }
}
