{
  "name": "@decade/testing",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "lint": "eslint src",
    "sharding:report": "node --import tsx src/kafka/commands-topic-sharding.report.ts",
    "test": "vitest run"
  }
}
